Instructions for Deploying on Local Machine
1) Run the SQL script inside DOCS folder on a local database (SQL Server).
2) Add Twilio SID, Token and Number in App.config file.
3) Needed Nuget packages will be downloaded automatically during first build.